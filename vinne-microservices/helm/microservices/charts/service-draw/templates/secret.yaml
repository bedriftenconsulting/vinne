apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.fullname" . }}-secret
  labels:
    {{- include "chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Database credentials
  DATABASE_PASSWORD: "Draw2025Secure"
  DATABASE_URL: "postgresql://draw:Draw2025Secure@postgres-draw.external-services.svc.cluster.local:5432/draw_service?sslmode=disable"

  # Redis password (from credentials-dev.txt)
  REDIS_PASSWORD: "Draw2025RedisPassword"

  # JWT configuration (must be same across all services)
  JWT_SECRET: "randlotteryltd-jwt-secret-h8f9x2w1v7q5k3m9n6b4c8d2e5f7g0j1"
