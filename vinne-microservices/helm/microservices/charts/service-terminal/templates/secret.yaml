apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.fullname" . }}-secret
  labels:
    {{- include "chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Database credentials
  DATABASE_PASSWORD: "Terminal2025Secure"
  DATABASE_URL: "postgresql://terminal:Terminal2025Secure@postgres-terminal.external-services.svc.cluster.local:5432/terminal_service?sslmode=disable"

  # Redis password (from credentials-dev.txt)
  REDIS_PASSWORD: "joimkBOC2ZNMV0VOioJwZ7d9RfhXhDs7"

  # JWT configuration (must be same across all services)
  JWT_SECRET: "randlotteryltd-jwt-secret-h8f9x2w1v7q5k3m9n6b4c8d2e5f7g0j1"
