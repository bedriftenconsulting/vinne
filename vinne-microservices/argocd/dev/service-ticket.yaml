apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: service-ticket-dev
  namespace: argocd
  labels:
    environment: dev
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd-image-updater.argoproj.io/image-list: service-ticket=ghcr.io/nexalabshq/rand-ticket:develop
    argocd-image-updater.argoproj.io/service-ticket.update-strategy: latest
    argocd-image-updater.argoproj.io/service-ticket.allow-tags: regexp:^develop-[a-f0-9]{7}$
    argocd-image-updater.argoproj.io/service-ticket.helm.image-name: image.tag
    argocd-image-updater.argoproj.io/service-ticket.helm.image-spec: tag
    argocd-image-updater.argoproj.io/write-back-method: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: microservices-dev
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  source:
    repoURL: https://github.com/nexalabshq/rand-microservices
    targetRevision: develop
    path: helm/microservices/charts/service-ticket
    helm:
      releaseName: service-ticket-dev
      valueFiles:
        - ../../values.yaml
        - ../../values-dev.yaml
      values: |
        fullnameOverride: service-ticket-dev
